# ERP

## Démarrer l'application

```shell
cp .env.example .env
```

N'oubliez pas de modifier le fichier `.env`...

```shell
docker compose up
```

## Réinitialiser l'applicaiton

```shell
docker compose down -v
```
